### Bug fix

Fixed an issue where Nexus would show update badges for tools whose latest release only contains assets for a different OS (e.g. a Windows-only release showing as an update on Linux). Nexus now checks that the latest release has a compatible installer before flagging it as an update.
