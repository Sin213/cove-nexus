### Bug Fixes

- **Fix child AppImage file opening (take 2):** Removed `LD_LIBRARY_PATH`, `LD_PRELOAD`, and `GDK_PIXBUF` vars from the launch environment entirely on Linux. Electron pollutes these even outside AppImage context, which broke xdg-open in child tools like Cove File Toolkit.
