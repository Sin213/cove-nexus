### Bug Fixes

- **Fix child AppImage file opening:** When Nexus runs as an AppImage, it no longer forwards Electron/AppImage-polluted `LD_LIBRARY_PATH` and `GDK_PIXBUF` vars to launched tools. This fixes "Open" (right-click) failing in child apps like Cove File Toolkit.
