### Bug Fixes

- **Clean up stale program versions on boot:** On Windows, running executables are file-locked and cannot be deleted during an update. Previously, the failed deletion was silently ignored and old versions accumulated in the programs directory. Nexus now sweeps the programs folder on startup and removes any cove-* artifact that is not the currently registered version for its slug.
